<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.IntroduceActivity">

    <com.youth.banner.Banner
        android:id="@+id/introduce_banner"
        app:is_auto_play="false"
        app:indicator_drawable_selected="@color/blue_theme"
        app:indicator_drawable_unselected="@color/grey_second"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></com.youth.banner.Banner>

    <TextView
        android:id="@+id/skip"
        android:text="跳过"
        android:textSize="@dimen/sp_14"
        android:layout_alignParentRight="true"
        android:padding="@dimen/dp_30"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/atonce"
        android:visibility="invisible"
        android:text="立即体验"
        android:textColor="@color/black_theme"
        android:background="@drawable/blue_oval_frame"
        android:gravity="center"
        android:layout_marginBottom="@dimen/dp_30"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_width="@dimen/dp_200"
        android:layout_height="@dimen/dp_50" />
</RelativeLayout>